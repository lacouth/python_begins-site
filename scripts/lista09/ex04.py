# Exercicio 04 — reajuste no patrimonio de equipamentos.
# TODO: pergunte o valor de 3 equipamentos (converta para float).
# TODO: monte uma lista com os tres valores, usando [].
# TODO: com for i in range(len(...)), aplique o reajuste de 5% em cada
#       item da lista (valores[i] = valores[i] * 1.05).
# TODO: com outro for i in range(len(...)), mostre "Equipamento <i + 1>:
#       R$ <valor>" para cada item, com duas casas decimais.
